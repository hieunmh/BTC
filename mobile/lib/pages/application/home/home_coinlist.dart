import 'package:flutter/material.dart';

class HomeCoinlist extends StatelessWidget {
  final List<dynamic> coinList;
  final String theme;
  
  const HomeCoinlist({
    super.key,
    required this.theme,
    required this.coinList
  });

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}